from setuptools import setup

if __name__ == '__main__':

    setup(name='ticker',
        version='0.1',
        description='Yahoo Finance Interface',
        packages=['ticker'],
        zip_safe=False)
